<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://Wipeher.github.io</id>
    <title>Gridea</title>
    <updated>2020-06-03T12:36:13.767Z</updated>
    <generator>https://github.com/jpmonette/feed</generator>
    <link rel="alternate" href="https://Wipeher.github.io"/>
    <link rel="self" href="https://Wipeher.github.io/atom.xml"/>
    <subtitle>温故而知新</subtitle>
    <logo>https://Wipeher.github.io/images/avatar.png</logo>
    <icon>https://Wipeher.github.io/favicon.ico</icon>
    <rights>All rights reserved 2020, Gridea</rights>
    <entry>
        <title type="html"><![CDATA[JDK 并发包]]></title>
        <id>https://Wipeher.github.io/post/jdk-bing-fa-bao/</id>
        <link href="https://Wipeher.github.io/post/jdk-bing-fa-bao/">
        </link>
        <updated>2020-06-01T07:51:31.000Z</updated>
        <content type="html"><![CDATA[<p>（本文作为笔记，书籍java高并发程序设计(葛一鸣)，java并发编程之美，精通java并发编程，极客时间专栏，java并发编程实战）</p>
<h4 id="并发编程可以总结为三个核心问题分工-同步-互斥"><em><strong>并发编程可以总结为三个核心问题：分工、同步、互斥</strong></em></h4>
<blockquote>
<blockquote>
<p>分工指的是如何高效的拆解任务并分配给线程</p>
</blockquote>
<blockquote>
<p>同步指的是线程之间如何协作</p>
</blockquote>
<blockquote>
<p>互斥则是保证同一时刻只允许一个线程访问共享资源</p>
</blockquote>
<p>Java SDK并发包很大程度上都是按照这三个维度组织的，例如Fork/Join框架就是一种分工模式，CountDownLatch就是一种典型的同步方式，而可重入锁则是一种互斥手段。</p>
</blockquote>
<h3 id="同步控制synchronized">同步控制Synchronized</h3>
<blockquote>
<p>其实Synchronized也是可重入锁</p>
</blockquote>
<h3 id="synchronized扩展重入锁reentrantlock">Synchronized扩展：重入锁ReentrantLock</h3>
<h3 id="锁相关联的condition条件变量">锁相关联的Condition条件变量</h3>
<h3 id="锁的扩展semaphore信号量">锁的扩展：Semaphore（信号量）</h3>
<blockquote>
<p>允许多个线程同时访问</p>
</blockquote>
<h3 id="readwritelock读写锁">ReadWriteLock读写锁</h3>
<h3 id="倒计数器countdownlatch">倒计数器：CountDownLatch</h3>
<blockquote>
<p>门闩</p>
</blockquote>
<h3 id="循环栅栏-cyclicbarrier">循环栅栏: CyclicBarrier</h3>
<blockquote>
<p>和CountDownLatch类似，都可以实现线程间的计数等待，但是功能更加强大。比如可以接收一个参数作为barrierAction，这个barrierAction就是当计数器</p>
</blockquote>
<h3 id="线程阻塞工具类locksupport">线程阻塞工具类：LockSupport</h3>
<blockquote>
<p>可以在任意位置阻塞线程，与Thread.suspend()方法相比，它弥补了由于resume()方法导致线程无法继续执行的情况。和Object.wait()相比，它不需要先获得某个对象的锁，也不会抛出InterruptedException异常。</p>
<p>LockSupport的静态方法park()可以阻塞当前线程，类似的还有parkNanos()、parkUntil()。这些实现了一个限时的等待。</p>
</blockquote>
<pre><code class="language-java">    
	public static void unpark(Thread thread) {
        if (thread != null)
            U.unpark(thread);
    }

    public static void park(Object blocker) {
        Thread t = Thread.currentThread();
        setBlocker(t, blocker);
        U.park(false, 0L);
        setBlocker(t, null);
    }

    public static void parkNanos(Object blocker, long nanos) {
        if (nanos &gt; 0) {
            Thread t = Thread.currentThread();
            setBlocker(t, blocker);
            U.park(false, nanos);
            setBlocker(t, null);
        }
    }

    public static void parkUntil(Object blocker, long deadline) {
        Thread t = Thread.currentThread();
        setBlocker(t, blocker);
        U.park(true, deadline);
        setBlocker(t, null);
    }
</code></pre>
<blockquote>
<p>LockSupport类使用了类似信号量的机制。为每一个线程准备了一个许可，如果许可可以使用，那么park()就会立即返回，并且消费这个许可（也就是把这个许可变为不可用），如果许可不可用，就会阻塞，而unpark()方法则使得一个许可变为可用（但是和信号量不同的是，许可不可以累加，不可能拥有超过一个许可，它永远只有一个）。</p>
<p>这个特点使得：即使unpark()发生在park()之前，它也可以使得下一次的park()方法操作立即返回。</p>
<p>show code:</p>
</blockquote>
<pre><code class="language-java">package test.day01.thread;

import java.util.concurrent.locks.LockSupport;

public class LockSupportDemo {

	public static Object u = new Object();
	
	static ChangeObjectThread t1 = new ChangeObjectThread(&quot;t1&quot;);
	static ChangeObjectThread t2 = new ChangeObjectThread(&quot;t2&quot;);
	
	
	public static class ChangeObjectThread extends Thread {
		public ChangeObjectThread(String name) {
			super.setName(name);
		}
		
		@Override
		public void run() {
			synchronized (u) {
				System.out.println(&quot;in&quot; + getName());
				LockSupport.park();
			}
		}
	}
	
	public static void main(String[] args) throws InterruptedException {
		t1.start();
		Thread.sleep(100);
		t2.start();
		LockSupport.unpark(t1);
		LockSupport.unpark(t2);
		t1.join();
		t2.join();
	}
	
}

</code></pre>
<p>运行结果：</p>
<pre><code class="language-java">int1
int2
</code></pre>
<blockquote>
<p>同时park()方法挂起状态的线程不会像suspend()方法那样还给出一个令人费解的Runnable状态。它会非常明确的给出一个WAITING状态，甚至还会标注是park()方法引起的。</p>
<p>如果使用park(Object)函数，那么还可以为当前线程设置一个阻塞对象。这个阻塞对象会出现在线程Dump中。</p>
<p>除了有定时阻塞的功能，LockSupport.park()方法还能支持中断影响，但是和其他接收中断的函数不一样，LockSupport.park()方法不会抛出InterruptedException异常。它只会默默返回，但是我们可以从Thread.interrupted()等方法中获得中断标记。</p>
</blockquote>
<pre><code class="language-java">import java.util.concurrent.locks.LockSupport;

public class LockSupportIntDemo {
	public static Object uObject = new Object();
	static ChangeObjectThread t1 = new ChangeObjectThread(&quot;t1&quot;);
	static ChangeObjectThread t2 = new ChangeObjectThread(&quot;t2&quot;);
	
	public static class ChangeObjectThread extends Thread {
		public ChangeObjectThread(String name) {
			super.setName(name);
		}
		
		@Override
		public void run() {
			synchronized (uObject) {
				System.out.println(&quot;in&quot; + getName());
				LockSupport.park();
				if (Thread.interrupted()) {
					System.out.println(getName() + &quot;被中断了&quot;);
				}
			}
			System.out.println(getName() + &quot; 执行结束&quot;);
		}
	}
	
	public static void main(String[] args) throws InterruptedException {
		t1.start();
		Thread.sleep(100);
		t2.start();
		t1.interrupt();
		LockSupport.unpark(t2);
	}
	
}
</code></pre>
<p>运行结果：</p>
<pre><code class="language-java">int1
t1被中断了
int2
t1 执行结束
t2 执行结束

</code></pre>
<hr>
<h3 id="guava-和-ratelimiter限流">Guava 和 RateLimiter限流</h3>
<blockquote>
<p>任何应用和模块组件都有一定的访问速率上限，如果请求速率突破了这个上限，不但多余的请求无法处理，甚至会压垮系统使所有的请求都无法有效处理 。因此对请求进行限流是非常重要的。</p>
<p>一个简单的限流办法是给出一个单位时间，然后使用一个计数器counter统计单位时间内收到的请求数量，当请求数量超过门限时，余下的请求丢弃或者等待。但这种简单的算法有一个严重的问题，就是很难控制边界时间上的请求。假设时间单位是1秒，每秒请求不超过10个。假设在这一秒的前半秒一个请求也没有，而后半秒有10个，下一秒的前半秒又有十个，那么就相当于一秒钟有20个请求。而这明显违法了限流的要求。</p>
<p>这是一种简单粗暴的总数量限流而不是平均限流。</p>
</blockquote>
<p>在通用的情况下，常用的限流算法有两种：漏桶算法和令牌桶算法。</p>
<blockquote>
<blockquote>
<p><strong>漏桶算法的基本思想是：利用一个缓冲区，当有请求进入系统时，无论请求的速率如何，都先在缓存区内保存，然后以固定的流速流出缓存区进行处理。</strong></p>
<p><strong>漏桶算法的特点是无论外部请求压力如何，它总是以固定的流速处理数据，漏桶的容积和流出速率是该算法的两个重要参数。</strong></p>
</blockquote>
<blockquote>
<p><strong>令牌桶算法是一种反向的漏桶算法。在令牌桶算法中，桶中存放的不再是请求，而是令牌。处理程序只有拿到令牌后，才能对请求进行处理。如果没有令牌，那么处理程序要么丢弃请求，要么等待可用令牌。为了限制流速，该算法在每个单位时间产生一定量的请求存入桶中。</strong></p>
<p><strong>通常，桶的容量是有限的，比如，当令牌没有被消耗时，只能累计有限单位时间内的令牌。</strong></p>
</blockquote>
</blockquote>
<p>RateLimiter正是采用了令牌桶算法。</p>
<pre><code class="language-java">import com.google.common.util.concurrent.RateLimiter;

/**
 * 令牌桶算法
 * @author Administrator
 *
 */
public class RateLimiterDemo {

	static RateLimiter rateLimiter = RateLimiter.create(2);
	
	public static class Task implements Runnable{

		@Override
		public void run() {
			System.out.println(System.currentTimeMillis());
		}
		
	}
	
	public static void main(String[] args) {
		for (int i = 0; i &lt; 50; i++) {
			rateLimiter.acquire();
			new Thread(new Task()).start();
		}
	}
	
}

</code></pre>
<p>运行结果：</p>
<pre><code class="language-java">1590988427056
1590988427278
1590988428458
1590988428931
1590988428948
1590988429901
</code></pre>
<p>当使用accquire()方法上来控制流量时，过剩的流量调用会等待，直到有机会执行。</p>
<p>也可以使用tryAcquire()方法，直接丢弃请求，避免系统无法处理请求，导致系统崩溃。</p>
<pre><code class="language-java">		for (int i = 0; i &lt; 50; i++) {
			if(!rateLimiter.tryAcquire()){
				continue;
			}
			new Thread(new Task()).start();
		}
</code></pre>
<p>当请求成功时，tryAcquire()返回true，否则返回false，该方法不会阻塞。</p>
<pre><code class="language-java">import com.google.common.util.concurrent.RateLimiter;

/**
 * 令牌桶算法
 * @author Administrator
 *
 */
public class RateLimiterDemo {

	static RateLimiter rateLimiter = RateLimiter.create(2);
	
	public static class Task implements Runnable{

		@Override
		public void run() {

			System.out.println(System.currentTimeMillis());
		}
		
	}
	
	public static void main(String[] args) {
		for (int i = 0; i &lt; 50; i++) {
			//rateLimiter.acquire();
			if (!rateLimiter.tryAcquire()) {
				continue;
			}
			new Thread(new Task()).start();
		}	
	}	
}
</code></pre>
<p>运行结果：</p>
<pre><code class="language-java">1590988878309
</code></pre>
<p>因为limiter限制的是1秒两次调用.也就是每500毫秒产生一个令牌，显然for循环的效率很高，因此本段代码最终只产生一个输出。</p>
<h3 id="线程复用线程池">线程复用：线程池</h3>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[java并发设计-基础概念]]></title>
        <id>https://Wipeher.github.io/post/java-bing-fa-she-ji-ji-chu-gai-nian/</id>
        <link href="https://Wipeher.github.io/post/java-bing-fa-she-ji-ji-chu-gai-nian/">
        </link>
        <updated>2020-05-23T09:06:51.000Z</updated>
        <content type="html"><![CDATA[<!-- more -->
<h4 id="同步synchronous和异步asynchronous">同步(Synchronous)和异步(Asynchronous)</h4>
<blockquote>
<p>同步指的是方法调用，必须要等方法调用返回之后，才能继续后续其他行为。</p>
<p>异步指的是方法调用，开始调用可以立即返回。调用者可以继续后续的行为。异步方法通常另开一个线程执行。而且被调用方通过其他方式通知调用者。</p>
</blockquote>
<p>有意思的是，一般情况，大家会把同步异步和阻塞非阻塞放在一起组合。也很容易混淆。</p>
<h4 id="阻塞blocking和非阻塞non-blocking">阻塞(Blocking)和非阻塞(Non-Blocking)</h4>
<blockquote>
<p>阻塞指的是调用方在等待调用方法返回之前，需要一直等待结果返回。</p>
<p>非阻塞指的是不不管调用是否返回，都去执行其他行为。</p>
</blockquote>
<p>看到这里就有一种同步等于阻塞，异步等于非阻塞的错觉</p>
<p>这里引用陈硕在知乎的回答：怎样理解阻塞非阻塞与同步异步的区别？ - 陈硕的回答 - 知乎 https://www.zhihu.com/question/19732473/answer/26091478</p>
<p>在处理IO的时候，阻塞和非阻塞都是同步IO，</p>
<p>只有使用了特殊的API才是异步IO</p>
<hr>
<h4 id="并发concurrency和并行parallelism">并发(Concurrency)和并行(parallelism)</h4>
<blockquote>
<p>并发和并行是两个非常容易被混淆的概念。</p>
<p>并发是指同一时刻只能有一条指令执行，但是多个线程的对应的指令被快速交替轮换地执行。</p>
<p>并行是真正意义上的同时执行，指同一时刻，有多条指令在多个处理器上同时执行。并行必须要依赖于多个处理器。不论是从宏观上还是微观上。</p>
</blockquote>
<hr>
<h4 id="临界区">临界区</h4>
<p>临界区用来表示一种公共资源或者共享数据，可以被多个线程使用。每一次，都只有一个线程能够使用它。临界区资源如果被占用，其他线程必须等待。</p>
<hr>
<p>死锁(DeadLock)、 饥饿(Starvation)和活锁(Livelock)</p>
<blockquote>
<p>死锁，与其他锁互相依赖等待释放，结果导致都不能释放。</p>
<p>饥饿是指某一个或者多个线程因为种种原因无法获得所需要的资源，导致一直无法执行。比如线程优先级可能太低，而高优先级的线程不断抢占它需要的资源，导致低优先级的线程无法工作。</p>
<p>(在Linux系统编程一书中提到，不合适的编码，一样可能导致线程优先级低的线程抢占线程优先级高的线程)</p>
<p>活锁这种现象比较少见，都是主动将资源让给其他线程，最终导致多个线程都拿不到资源，没有一个线程可以执行。</p>
</blockquote>
<hr>
<p><strong>根据控制并发的策略，可以把并发分成几个不同的级别</strong></p>
<blockquote>
<ol>
<li>阻塞</li>
</ol>
<blockquote>
<p>一个线程阻塞，则其他线程释放资源之前，当前线程无法继续执行。</p>
</blockquote>
<ol start="2">
<li>
<p>无饥饿(Starbation-Free)</p>
<blockquote>
<p>线程之间有优先级，那么线程调度时，会尽量满足线程优先级高的线程(请注意：这里用的是尽量，因为线程优先级并不总是有效)</p>
<p>当然锁也有公平锁和非公平锁的区分。</p>
</blockquote>
</li>
<li>
<p>无障碍(Obstruction-Free)</p>
<blockquote>
<p>其实可以理解为乐观锁的理论依据</p>
</blockquote>
</li>
<li>
<p>无锁(Lock-Free)</p>
<blockquote>
<p>无锁的并行都是无障碍的。在无锁的情况下，所有的线程都尝试对临界区进行访问。</p>
</blockquote>
</li>
<li>
<p>无等待(Wait-Free)</p>
<blockquote>
<p>无锁只要求一个线程可以在有限步执行完，而无等待则在无锁的基础上更进一步。要求所有的线程都必须在有限步内完成。</p>
</blockquote>
</li>
</ol>
</blockquote>
]]></content>
    </entry>
</feed>